// eslint-disable-next-line no-unused-vars
import React from 'react';
import EmailValidationForm from "../components/authentication /EmailValidationForm.jsx";

const EmailValidation = () => {
    return (
        <div>
            <EmailValidationForm />
        </div>
    );
};

export default EmailValidation;
